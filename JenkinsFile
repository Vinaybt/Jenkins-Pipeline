pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/Vinaybt/Jenkins-Pipeline.git'
            }
        }
        stage('Build C++ App') {
            steps {
                sh 'make'
            }
        }
        stage('Run App') {
            steps {
                sh './app'
            }
        }
        stage('Clean Up') {
            steps {
                sh 'make clean'
            }
        }
    }
}
