pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                git branch: 'main', url: 'https://github.com/Vinaybt/Jenkins-Pipeline.git'
            }
        }
        stage('Build C++ App') {
            steps {
                sh './MakeFile'
            }
        }
        stage('Run App') {
            steps {
                sh './app'
            }
        }
        stage('Clean Up') {
            steps {
                sh 'make clean'
            }
        }
    }
}
